<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Petly.Maui.Views.AdoptionPage"
             Title="Адопція"
             BackgroundColor="#FFFFFBF7">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="20">

            <!-- Назад -->
            <Button Text="← Назад"
                    BackgroundColor="#FFFFD129"
                    TextColor="Black"
                    BorderWidth="1"
                    CornerRadius="8"
                    WidthRequest="100"
                    Clicked="OnBackClicked"/>

            <Label Text="Адопція"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="Black"/>

            <!-- Мої дані -->
            <Label Text="Мої дані" FontSize="20" FontAttributes="Bold" TextColor="Black"/>
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="15" RowSpacing="10">
                <Entry Placeholder="Ім'я" Grid.Row="0" Grid.Column="0" />
                <Entry Placeholder="Прізвище" Grid.Row="0" Grid.Column="1" />
                <Entry Placeholder="Email" Grid.Row="1" Grid.Column="0" Keyboard="Email" />
                <Entry Placeholder="Телефон +380" Grid.Row="1" Grid.Column="1" Keyboard="Telephone" />
            </Grid>

            <!-- Тваринка -->
            <Label Text="Тваринка" FontSize="20" FontAttributes="Bold" TextColor="Black" Margin="0,10,0,0"/>
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="15" RowSpacing="10">

                <!-- Ім’я -->
                <Entry Placeholder="Ім’я тварини" Grid.Row="0" Grid.Column="0" />

                <!-- Вид -->
                <Entry Placeholder="Вид (кіт, собака...)" Grid.Row="0" Grid.Column="1" />

                <!-- Вік -->
                <Entry Placeholder="Вік (наприклад: 2 роки)" Grid.Row="1" Grid.Column="0" />

                <!-- Стать -->
                <Picker Grid.Row="1" Grid.Column="1"
                        Title="Стать"
                        BackgroundColor="White"
                        TextColor="Black">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Самець ♂</x:String>
                            <x:String>Самка ♀</x:String>
                            <x:String>Невідомо ⚪</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </Grid>

            <!-- Кнопка -->
            <Button Text="Надіслати заявку"
                    BackgroundColor="#FFFFD129"
                    TextColor="Black"
                    FontAttributes="Bold"
                    CornerRadius="10"
                    HeightRequest="45"
                    Clicked="OnSubmitClicked"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
